import React from 'react';
import { Card } from '@mui/material';

interface IPlaceCard {
  placeName: string;
  placeDesc: string;
}
const PlaceCard = () => {
  return <Card></Card>;
};

export default PlaceCard;

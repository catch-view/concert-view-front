import { Box, styled } from '@mui/material';

export const TagsArea = styled(Box)({
  display: 'flex',
  flexDirection: 'column',
  borderRadius: '0.5rem',
  marginTop: '0.5rem',
});

export const TagsBox = styled(Box)({
  display: 'flex',
  flexDirection: 'row',
  marginTop: '0.5rem',
});
